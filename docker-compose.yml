services:
  easyrubero-web:
    build: .
    container_name: easyrubero_web
    restart: always
    env_file:
      - .env
    ports:
      - "1999:1999"
    environment:
      - NODE_ENV=production
      - PORT=1999
